# Campus Bus test coverage

Issue #668 reviewed all 18 behaviours that previously lived in
`e2e/campus-bus.spec.ts`. Browser coverage remains only where the behaviour
depends on real navigation, network traffic, browser state, permissions, map
interaction, or responsive geometry.

| #   | Original behaviour                                                   | Layer and destination                                                                                                                                                              | Why                                                                                                                   |
| --- | -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| 1   | Testing entry is visible without exposing model operations           | Component/unit: `tests/components/navbar.test.tsx`; `tests/lib/campus-transport-prediction-model-cache.test.ts`                                                                    | Static navigation label and rollout rule do not need a browser.                                                       |
| 2   | Catalog fills the main shell width                                   | E2E: `campus bus catalog layout › fills the available main shell width`                                                                                                            | Requires real layout geometry.                                                                                        |
| 3   | Model lab is unavailable during feedback-only rollout                | E2E: `campus bus model lab rollout › keeps the lab unavailable during feedback-only rollout`                                                                                       | Verifies the real `/campus-bus/lab` navigation and 404 boundary.                                                      |
| 4   | Route 2 shows its default stop, map, timetable, and passenger caveat | E2E: `campus bus Route 2 mobile journey › renders the default stop, route map, and timetable`; component: `tests/components/campus-route-view.test.tsx`                            | The responsive viewport/map journey remains E2E; static copy and stop-board rules are independently covered in jsdom. |
| 5   | LandsD tiles are used and OSM is not                                 | E2E: `uses the reviewed LandsD basemap instead of the blocked OSM tile service`                                                                                                    | Requires observing real browser requests.                                                                             |
| 6   | One failed raster tile does not disable the map                      | E2E: `keeps the map usable when one raster tile fails`                                                                                                                             | Requires browser request interception and the rendered map runtime.                                                   |
| 7   | Selecting a partial-service stop synchronizes map and board          | E2E: `selecting a partial-service stop keeps the map and stop board synchronized`                                                                                                  | Cross-component responsive interaction depends on the real map.                                                       |
| 8   | Geolocation offers a choice between opposite-side stops              | E2E: `asks the user to choose between nearby stops on opposite sides of the road`                                                                                                  | Requires browser geolocation permission and map/board interaction.                                                    |
| 9   | Arrival feedback adjusts minutes and submits once                    | E2E: `adjusts the reported minute and acknowledges one submission once`                                                                                                            | Verifies the real POST payload, response, dialog, and toast lifecycle.                                                |
| 10  | Anonymous rate limiting is explained                                 | E2E: `explains when anonymous arrival feedback is submitted too frequently`                                                                                                        | Verifies a browser-level 429 response and user-facing error path.                                                     |
| 11  | Nearby results survive route navigation and return                   | E2E: `keeps nearby results after opening a route and returning home`                                                                                                               | Depends on real navigation and client-session state.                                                                  |
| 12  | Manual boarding-place selection remains after location denial        | E2E: `keeps manual Boarding place selection usable after location is denied`                                                                                                       | Depends on the browser geolocation failure path and responsive dialog interaction.                                    |
| 13  | Before service, the first departure replaces a multi-hour countdown  | Component: `tests/components/campus-route-view.test.tsx`; unit: `tests/lib/campus-transport-routes.test.ts`; E2E: `keeps before-service feedback reachable in the mobile viewport` | Time and copy stay lightweight; the original responsive viewport assertion remains in Playwright.                     |
| 14  | Reviewed routes are listed and Route 3 opens                         | Component: `tests/components/campus-bus-home.test.tsx`, `tests/components/campus-bus-route-list.test.tsx`; E2E: `opens Route 3 from the reviewed catalog`                          | Tab switching and the exhaustive catalog run in jsdom; one real navigation remains E2E.                               |
| 15  | All routes remain available when none is running                     | Component: `tests/components/campus-bus-home.test.tsx`, `tests/components/campus-bus-route-list.test.tsx`; unit: `tests/lib/campus-bus-catalog.test.ts`                            | Tab access, pure catalog grouping, and static empty-state presentation do not need a browser.                         |
| 16  | Route 5 is unavailable during reading week                           | Component: `tests/components/campus-route-view.test.tsx`; unit: `tests/lib/campus-transport-routes.test.ts`                                                                        | Calendar/service-day business rule and static status copy.                                                            |
| 17  | A colocated loop marker cycles to the other occurrence               | E2E: `cycles colocated loop occurrences from one map marker`                                                                                                                       | Requires the real map marker and scroll/selection interaction.                                                        |
| 18  | N route labels PGH1 as conditional, not universal                    | Component: `tests/components/campus-route-view.test.tsx`; unit: `tests/lib/campus-transport-routes.test.ts`; E2E: `renders the reviewed N route in the real map`                   | Static labeling stays lightweight; route-specific real-map rendering remains in Playwright.                           |

Result: 15 focused browser journeys remain. Static and rule assertions from five
behaviours moved to lighter layers; three journeys left Playwright entirely,
while the browser-specific assertions in behaviours 13 and 18 remain E2E.
