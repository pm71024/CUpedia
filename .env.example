# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/cuclaw

# Auth
AUTH_SECRET=dev-secret-change-in-production
AUTH_URL=http://localhost:3000
# Bearer secret for the campus-bus train-model internal endpoint.
# CRON_SECRET=generate-a-dedicated-random-secret
# Production-only bearer secret for the one-source menu sync trigger. Configure
# the identical value as the GitHub Actions MENU_SYNC_TRIGGER_SECRET secret.
# MENU_SYNC_TRIGGER_SECRET=generate-a-dedicated-random-secret

# Campus Map — restrict the browser-visible Web key to deployed domains.
# The security code remains server-only and is injected by /_AMapService.
AMAP_WEB_KEY=your-amap-web-js-api-key
AMAP_SECURITY_JS_CODE=your-amap-security-js-code

# Optional: refresh production wiki caches after a Notion import
# Set both values together when importing directly into production.
# WIKI_REVALIDATE_URL=https://cupedia.org/api/internal/revalidate-wiki
# WIKI_REVALIDATE_SECRET=generate-a-dedicated-random-secret

# Brevo email delivery
BREVO_API_KEY=your-brevo-api-key
EMAIL_FROM=noreply@cupedia.org

# Dev-only: bypass @cuhk.edu.hk email whitelist
SKIP_EMAIL_WHITELIST=true

# Dev-only: canteen in-memory data (no PostgreSQL required for P0 preview)
# CANTEEN_MOCK_DATA=true

# Canteen voting (anonymous session + rate limits)
# CANTEEN_ANON_SESSION_TTL_SECONDS=3600
# CANTEEN_VOTE_RATE_LIMIT_PER_MIN=60

# Homepage danmaku: max posts per user per hour (default 5)
DANMAKU_RATE_LIMIT_PER_HOUR=5

# Anonymous campus-bus arrival feedback (default 12 writes / 10 minutes per
# signed one-hour anonymous session; no network identifier is stored).
# CAMPUS_BUS_FEEDBACK_RATE_LIMIT_PER_10_MIN=12

# Keep disabled during the feedback-only rollout. Enabling this exposes the
# model lab and allows experiment, training, promotion, and rollback writes.
# CAMPUS_BUS_MODEL_OPERATIONS_ENABLED=true

# Sensitive-word filter on comment / danmaku publish (default: on)
# SENSITIVE_CONTENT_FILTER=false

# Storage (S3-compatible: MinIO / Cloudflare R2 / AWS S3)
# MinIO:  MINIO_ENDPOINT=localhost  MINIO_PORT=9000
# R2/S3:  MINIO_ENDPOINT=https://<account>.r2.cloudflarestorage.com  (omit MINIO_PORT)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_REGION=us-east-1
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=cuclaw-uploads
MINIO_PRIVATE_BUCKET=cuclaw-private-uploads
MINIO_PUBLIC_URL=http://localhost:9000/cuclaw-uploads
