@AGENTS.md

## Agent skills

### Issue tracker

GitHub Issues on `HomuraCatMadoka/CUpedia` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles with default label strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout via `CONTEXT-MAP.md` with per-context `CONTEXT.md` files and shared `docs/adr/`. See `docs/agents/domain.md`.
